{
  "name": "localforage-webextensionstorage-driver",
  "version": "1.0.0",
  "description": "LocalForage storage driver for WebExtension chrome.storage APIs",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage",
    "build": "babel src --out-dir .",
    "prepublish": "npm test; npm run build"
  },
  "files": [
    "local.js",
    "sync.js"
  ],
  "author": "Espen Henriksen",
  "license": "MIT",
  "peerDependencies": {
    "localforage": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "jest": "^17.0.3",
    "localforage": "^1.4.3"
  },
  "jest": {
    "testURL": "https://www.example.com",
    "setupFiles": [
      "<rootDir>/setupTests.js"
    ]
  }
}
